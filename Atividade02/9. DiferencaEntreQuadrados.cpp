#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a, b;
    cin >> a >> b;

    float resultado = pow(a,2) - pow(b,2);

    cout << "Resultado = " << resultado << endl;
}